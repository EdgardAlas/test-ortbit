import React from "react"
import Button from "../../Button"
import Section from "../../Section"

const FreeProposal = () => {
  return (
    <Section blue>
      <section className="proposal-section">
        <Button orange noIcon>
          Click here to get your free proposal
        </Button>
      </section>
    </Section>
  )
}

export default FreeProposal
